[project]
name = "opportunity-finder-ca"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
    "beautifulsoup4>=4.12",
    "requests>=2.31",
    "openai>=1.0",
    "flask>=3.0",
    "upstash-redis>=1.0",
]

[tool.vercel]
entrypoint = "api.search:app"
